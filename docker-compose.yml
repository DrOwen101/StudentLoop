services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: studentloop
      POSTGRES_PASSWORD: studentloop
      POSTGRES_DB: studentloop
    ports:
      - "5432:5432"
    volumes:
      - studentloop_data:/var/lib/postgresql/data
volumes:
  studentloop_data:
